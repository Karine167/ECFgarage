{% extends 'base.html.twig' %}

{% block title %} Garage Vincent Parrot - Contact {% endblock %}

{% block body %}
    
    <div class="container m-4 ">
        <h4> Formulaire de contact </h4>
        <div>
            {% for message in app.flashes('success') %}
                <div class="alert alert-success mt-4">
                    {{message}}
                <div>
            {% endfor %}
        </div>

        {{form(form)}}
    </div>
{% endblock %}
